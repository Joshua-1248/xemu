Debug Tools — Phase 4 independent export manifest

COPY DIRECTORY:
  xemu-features/debug-tools/

BUILD OPTION:
  xemu_feature_debug_tools

CONFIG MACRO:
  CONFIG_XEMU_FEATURE_DEBUG_TOOLS

CORE/FRONTEND INTEGRATION SITES:
- ui/xui/main.cc — custom debug windows hook.
- ui/xui/menubar.cc — custom debug menu items.
- ui/xui/meson.build — conditional UI/disassembler sources.
- root meson.build — conditional debug-api.c and shared guest-memory.

See README.md for API, dependencies, threading, hot-path and disabled behavior.
